// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
// Code generated by Microsoft (R) Go Code Generator. DO NOT EDIT.

package fake

import (
	"errors"
	"fmt"
	"github.com/Azure/azure-sdk-for-go/sdk/azcore/runtime"
	"net/http"
	"strings"
	"sync"
)

// Server is a fake server for instances of the rawjson.Client type.
type Server struct {
	// InputOnlyServer contains the fakes for client InputOnlyClient
	InputOnlyServer InputOnlyServer

	// ModelCaseServer contains the fakes for client ModelCaseClient
	ModelCaseServer ModelCaseServer

	// OutputOnlyServer contains the fakes for client OutputOnlyClient
	OutputOnlyServer OutputOnlyServer
}

// NewServerTransport creates a new instance of ServerTransport with the provided implementation.
// The returned ServerTransport instance is connected to an instance of rawjson.Client via the
// azcore.ClientOptions.Transporter field in the client's constructor parameters.
func NewServerTransport(srv *Server) *ServerTransport {
	return &ServerTransport{srv: srv}
}

// ServerTransport connects instances of rawjson.Client to instances of Server.
// Don't use this type directly, use NewServerTransport instead.
type ServerTransport struct {
	srv                *Server
	trMu               sync.Mutex
	trInputOnlyServer  *InputOnlyServerTransport
	trModelCaseServer  *ModelCaseServerTransport
	trOutputOnlyServer *OutputOnlyServerTransport
}

// Do implements the policy.Transporter interface for ServerTransport.
func (s *ServerTransport) Do(req *http.Request) (*http.Response, error) {
	rawMethod := req.Context().Value(runtime.CtxAPINameKey{})
	method, ok := rawMethod.(string)
	if !ok {
		return nil, nonRetriableError{errors.New("unable to dispatch request, missing value for CtxAPINameKey")}
	}

	return s.dispatchToClientFake(req, method[:strings.Index(method, ".")])
}

func (s *ServerTransport) dispatchToClientFake(req *http.Request, client string) (*http.Response, error) {
	var resp *http.Response
	var err error

	switch client {
	case "InputOnlyClient":
		initServer(&s.trMu, &s.trInputOnlyServer, func() *InputOnlyServerTransport {
			return NewInputOnlyServerTransport(&s.srv.InputOnlyServer)
		})
		resp, err = s.trInputOnlyServer.Do(req)
	case "ModelCaseClient":
		initServer(&s.trMu, &s.trModelCaseServer, func() *ModelCaseServerTransport {
			return NewModelCaseServerTransport(&s.srv.ModelCaseServer)
		})
		resp, err = s.trModelCaseServer.Do(req)
	case "OutputOnlyClient":
		initServer(&s.trMu, &s.trOutputOnlyServer, func() *OutputOnlyServerTransport {
			return NewOutputOnlyServerTransport(&s.srv.OutputOnlyServer)
		})
		resp, err = s.trOutputOnlyServer.Do(req)
	default:
		err = fmt.Errorf("unhandled client %s", client)
	}

	return resp, err
}

// set this to conditionally intercept incoming requests to ServerTransport
var serverTransportInterceptor interface {
	// Do returns true if the server transport should use the returned response/error
	Do(*http.Request) (*http.Response, error, bool)
}
