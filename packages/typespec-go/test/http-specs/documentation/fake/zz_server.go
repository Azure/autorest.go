// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
// Code generated by Microsoft (R) Go Code Generator. DO NOT EDIT.

package fake

import (
	"errors"
	"fmt"
	"github.com/Azure/azure-sdk-for-go/sdk/azcore/runtime"
	"net/http"
	"strings"
	"sync"
)

// Server is a fake server for instances of the documentation.Client type.
type Server struct {
	// ListsServer contains the fakes for client ListsClient
	ListsServer ListsServer

	// TextFormattingServer contains the fakes for client TextFormattingClient
	TextFormattingServer TextFormattingServer
}

// NewServerTransport creates a new instance of ServerTransport with the provided implementation.
// The returned ServerTransport instance is connected to an instance of documentation.Client via the
// azcore.ClientOptions.Transporter field in the client's constructor parameters.
func NewServerTransport(srv *Server) *ServerTransport {
	return &ServerTransport{srv: srv}
}

// ServerTransport connects instances of documentation.Client to instances of Server.
// Don't use this type directly, use NewServerTransport instead.
type ServerTransport struct {
	srv                    *Server
	trMu                   sync.Mutex
	trListsServer          *ListsServerTransport
	trTextFormattingServer *TextFormattingServerTransport
}

// Do implements the policy.Transporter interface for ServerTransport.
func (s *ServerTransport) Do(req *http.Request) (*http.Response, error) {
	rawMethod := req.Context().Value(runtime.CtxAPINameKey{})
	method, ok := rawMethod.(string)
	if !ok {
		return nil, nonRetriableError{errors.New("unable to dispatch request, missing value for CtxAPINameKey")}
	}

	return s.dispatchToClientFake(req, method[:strings.Index(method, ".")])
}

func (s *ServerTransport) dispatchToClientFake(req *http.Request, client string) (*http.Response, error) {
	var resp *http.Response
	var err error

	switch client {
	case "ListsClient":
		initServer(&s.trMu, &s.trListsServer, func() *ListsServerTransport {
			return NewListsServerTransport(&s.srv.ListsServer)
		})
		resp, err = s.trListsServer.Do(req)
	case "TextFormattingClient":
		initServer(&s.trMu, &s.trTextFormattingServer, func() *TextFormattingServerTransport {
			return NewTextFormattingServerTransport(&s.srv.TextFormattingServer)
		})
		resp, err = s.trTextFormattingServer.Do(req)
	default:
		err = fmt.Errorf("unhandled client %s", client)
	}

	return resp, err
}

// set this to conditionally intercept incoming requests to ServerTransport
var serverTransportInterceptor interface {
	// Do returns true if the server transport should use the returned response/error
	Do(*http.Request) (*http.Response, error, bool)
}
