// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
// Code generated by Microsoft (R) Go Code Generator. DO NOT EDIT.

package fake

import (
	"errors"
	"fmt"
	azfake "github.com/Azure/azure-sdk-for-go/sdk/azcore/fake"
	"github.com/Azure/azure-sdk-for-go/sdk/azcore/fake/server"
	"github.com/Azure/azure-sdk-for-go/sdk/azcore/runtime"
	"github.com/Azure/azure-sdk-for-go/sdk/azcore/to"
	"net/http"
	"nextlinkverbgroup"
)

// NextLinkVerbServer is a fake server for instances of the nextlinkverbgroup.NextLinkVerbClient type.
type NextLinkVerbServer struct {
	// NewListItemsPager is the fake for method NextLinkVerbClient.NewListItemsPager
	// HTTP status codes to indicate success: http.StatusOK
	NewListItemsPager func(options *nextlinkverbgroup.NextLinkVerbClientListItemsOptions) (resp azfake.PagerResponder[nextlinkverbgroup.NextLinkVerbClientListItemsResponse])
}

// NewNextLinkVerbServerTransport creates a new instance of NextLinkVerbServerTransport with the provided implementation.
// The returned NextLinkVerbServerTransport instance is connected to an instance of nextlinkverbgroup.NextLinkVerbClient via the
// azcore.ClientOptions.Transporter field in the client's constructor parameters.
func NewNextLinkVerbServerTransport(srv *NextLinkVerbServer) *NextLinkVerbServerTransport {
	return &NextLinkVerbServerTransport{
		srv:               srv,
		newListItemsPager: newTracker[azfake.PagerResponder[nextlinkverbgroup.NextLinkVerbClientListItemsResponse]](),
	}
}

// NextLinkVerbServerTransport connects instances of nextlinkverbgroup.NextLinkVerbClient to instances of NextLinkVerbServer.
// Don't use this type directly, use NewNextLinkVerbServerTransport instead.
type NextLinkVerbServerTransport struct {
	srv               *NextLinkVerbServer
	newListItemsPager *tracker[azfake.PagerResponder[nextlinkverbgroup.NextLinkVerbClientListItemsResponse]]
}

// Do implements the policy.Transporter interface for NextLinkVerbServerTransport.
func (n *NextLinkVerbServerTransport) Do(req *http.Request) (*http.Response, error) {
	rawMethod := req.Context().Value(runtime.CtxAPINameKey{})
	method, ok := rawMethod.(string)
	if !ok {
		return nil, nonRetriableError{errors.New("unable to dispatch request, missing value for CtxAPINameKey")}
	}

	return n.dispatchToMethodFake(req, method)
}

func (n *NextLinkVerbServerTransport) dispatchToMethodFake(req *http.Request, method string) (*http.Response, error) {
	resultChan := make(chan result)
	defer close(resultChan)

	go func() {
		var intercepted bool
		var res result
		if nextLinkVerbServerTransportInterceptor != nil {
			res.resp, res.err, intercepted = nextLinkVerbServerTransportInterceptor.Do(req)
		}
		if !intercepted {
			switch method {
			case "NextLinkVerbClient.NewListItemsPager":
				res.resp, res.err = n.dispatchNewListItemsPager(req)
			default:
				res.err = fmt.Errorf("unhandled API %s", method)
			}

		}
		select {
		case resultChan <- res:
		case <-req.Context().Done():
		}
	}()

	select {
	case <-req.Context().Done():
		return nil, req.Context().Err()
	case res := <-resultChan:
		return res.resp, res.err
	}
}

func (n *NextLinkVerbServerTransport) dispatchNewListItemsPager(req *http.Request) (*http.Response, error) {
	if n.srv.NewListItemsPager == nil {
		return nil, &nonRetriableError{errors.New("fake for method NewListItemsPager not implemented")}
	}
	newListItemsPager := n.newListItemsPager.get(req)
	if newListItemsPager == nil {
		resp := n.srv.NewListItemsPager(nil)
		newListItemsPager = &resp
		n.newListItemsPager.add(req, newListItemsPager)
		server.PagerResponderInjectNextLinks(newListItemsPager, req, func(page *nextlinkverbgroup.NextLinkVerbClientListItemsResponse, createLink func() string) {
			page.NextLink = to.Ptr(createLink())
		})
	}
	resp, err := server.PagerResponderNext(newListItemsPager, req)
	if err != nil {
		return nil, err
	}
	if !contains([]int{http.StatusOK}, resp.StatusCode) {
		n.newListItemsPager.remove(req)
		return nil, &nonRetriableError{fmt.Errorf("unexpected status code %d. acceptable values are http.StatusOK", resp.StatusCode)}
	}
	if !server.PagerResponderMore(newListItemsPager) {
		n.newListItemsPager.remove(req)
	}
	return resp, nil
}

// set this to conditionally intercept incoming requests to NextLinkVerbServerTransport
var nextLinkVerbServerTransportInterceptor interface {
	// Do returns true if the server transport should use the returned response/error
	Do(*http.Request) (*http.Response, error, bool)
}
