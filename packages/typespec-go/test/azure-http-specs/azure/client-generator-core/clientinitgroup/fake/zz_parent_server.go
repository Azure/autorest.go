// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
// Code generated by Microsoft (R) Go Code Generator. DO NOT EDIT.

package fake

import (
	"errors"
	"fmt"
	"github.com/Azure/azure-sdk-for-go/sdk/azcore/runtime"
	"net/http"
	"strings"
	"sync"
)

// ParentServer is a fake server for instances of the clientinitgroup.ParentClient type.
type ParentServer struct {
	// ParentChildServer contains the fakes for client ParentChildClient
	ParentChildServer ParentChildServer
}

// NewParentServerTransport creates a new instance of ParentServerTransport with the provided implementation.
// The returned ParentServerTransport instance is connected to an instance of clientinitgroup.ParentClient via the
// azcore.ClientOptions.Transporter field in the client's constructor parameters.
func NewParentServerTransport(srv *ParentServer) *ParentServerTransport {
	return &ParentServerTransport{srv: srv}
}

// ParentServerTransport connects instances of clientinitgroup.ParentClient to instances of ParentServer.
// Don't use this type directly, use NewParentServerTransport instead.
type ParentServerTransport struct {
	srv                 *ParentServer
	trMu                sync.Mutex
	trParentChildServer *ParentChildServerTransport
}

// Do implements the policy.Transporter interface for ParentServerTransport.
func (p *ParentServerTransport) Do(req *http.Request) (*http.Response, error) {
	rawMethod := req.Context().Value(runtime.CtxAPINameKey{})
	method, ok := rawMethod.(string)
	if !ok {
		return nil, nonRetriableError{errors.New("unable to dispatch request, missing value for CtxAPINameKey")}
	}

	return p.dispatchToClientFake(req, method[:strings.Index(method, ".")])
}

func (p *ParentServerTransport) dispatchToClientFake(req *http.Request, client string) (*http.Response, error) {
	var resp *http.Response
	var err error

	switch client {
	case "ParentChildClient":
		initServer(&p.trMu, &p.trParentChildServer, func() *ParentChildServerTransport {
			return NewParentChildServerTransport(&p.srv.ParentChildServer)
		})
		resp, err = p.trParentChildServer.Do(req)
	default:
		err = fmt.Errorf("unhandled client %s", client)
	}

	return resp, err
}

// set this to conditionally intercept incoming requests to ParentServerTransport
var parentServerTransportInterceptor interface {
	// Do returns true if the server transport should use the returned response/error
	Do(*http.Request) (*http.Response, error, bool)
}
